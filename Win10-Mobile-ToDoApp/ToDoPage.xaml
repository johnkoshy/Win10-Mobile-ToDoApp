<Page
    x:Class="Win10_Mobile_ToDoApp.ToDoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win10_Mobile_ToDoApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBox x:Name="TaskInput" PlaceholderText="Enter a task" Margin="0,0,0,10"/>
            <Button x:Name="AddTaskButton" Content="Add Task" Click="AddTaskButton_Click"/>
        </StackPanel>

        <ListView x:Name="TaskListView" Grid.Row="1" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TaskItem">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{x:Bind IsCompleted, Mode=TwoWay}"/>
                        <TextBlock Text="{x:Bind TaskName}" Margin="5,0"/>
                        <Button Content="Delete" Tag="{x:Bind TaskName}" Click="DeleteTaskButton_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>