<Page x:Class="Win10_Mobile_ToDoApp.ToDoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Win10_Mobile_ToDoApp"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Page.Resources>
        <local:BoolToTextDecorationsConverter x:Key="BoolToTextDecorationsConverter"/>
        <Style x:Key="TaskTextStyleLight" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
        <Style x:Key="TaskTextStyleDark" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="AddButtonStyleDark" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <CompositeTransform/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <StackPanel Margin="20">
            <TextBox x:ConnectionId='2' x:Name="TaskInput" PlaceholderText="Enter task" Margin="0,0,0,10"/>
            <Button x:ConnectionId='3' x:Name="AddTaskButton" Content="Add Task"                             Margin="0,0,0,10" Style="{StaticResource AddButtonStyleDark}"/>
            <Button x:ConnectionId='4' x:Name="ThemeToggleButton" Content="Dark Mode"                                 Margin="0,0,0,10" Style="{StaticResource AddButtonStyleDark}"/>
            <ListView x:ConnectionId='5' x:Name="TaskListView">
                <ListView.ItemTemplate>
                    <DataTemplate                            >
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <CheckBox x:ConnectionId='8' IsChecked="{Binding IsCompleted, Mode=TwoWay}"                       />
                            <TextBlock x:Name="TaskTextBlock"
                                       Text="{Binding TaskName}"
                                       Style="{StaticResource TaskTextStyleLight}"
                                       TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToTextDecorationsConverter}, Mode=OneWay}"
                                       Margin="10,0"/>
                            <Button x:ConnectionId='10' x:Name="Delete" Tag="{Binding TaskName}"                                Content="Delete" Style="{StaticResource AddButtonStyleDark}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

